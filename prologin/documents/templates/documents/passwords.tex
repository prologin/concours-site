{% load i18n latex %}{% autoescape off %}
\documentclass[a4paper,11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{multicol}
\usepackage{framed}
\usepackage[margin=1.5cm]{geometry}

\setlength{\parskip}{0pt}
\setlength{\parsep}{0pt}
\setlength{\headsep}{0pt}
\setlength{\topskip}{0pt}
\setlength{\topmargin}{0pt}
\setlength{\topsep}{0pt}
\setlength{\partopsep}{0pt}
\setlength{\columnsep}{0pt}
\setlength{\voffset}{-0.75in}
\setlength{\headsep}{0pt}
\linespread{0.5}

\newcommand{\card}[3]{%
    \begin{framed}%
        \begin{tabular}{rl}%
            Nom          & #1 \\
            Login        & \texttt{#2} \\
            Mot de passe & \texttt{#3}
        \end{tabular}%
    \end{framed}%
}%

\begin{document}
\pagestyle{empty}

{% for center, wishes in items %}
\begin{center}
    \Large
    Liste des mots de passe -- Prologin {{ year }} -- {{ center.name|escapetex }}
\end{center}
\begin{multicols}{2}
\noindent%
{% for item in wishes %}{% with user=item.contestant.user %}%
\card{%
{{ user.get_full_name|escapetex }}%
}{%
{{ user.username|escapetex }}%
}{%
{{ user.plaintext_password|escapetex }}%
}%
{% endwith %}{% endfor %}%
\end{multicols}
\newpage

{% endfor %}
\end{document}
{% endautoescape %}
